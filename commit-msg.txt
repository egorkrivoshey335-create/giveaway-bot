feat(bot): Block 1 - BullMQ workers, Sentry, webhook mode

Block 1.11 - BullMQ jobs:
- Installed: bullmq, ioredis, @sentry/node
- Created Redis client for bot
- Winner notifications worker (queue: winner-notifications)
- Reminders worker (queue: reminders)
- Rate limiting: 30 msg/sec for notifications
- Batch processing for reminders (20 participants at a time)

Block 1.14 - Error handling:
- Sentry initialization with environment detection
- Error middleware for Grammy with Sentry integration
- Global error handlers (uncaughtException, unhandledRejection)
- Improved graceful shutdown (bot + redis)

Block 1.1 - Webhook mode:
- Support for webhook and polling modes (env WEBHOOK_ENABLED)
- Automatic webhook setup/deletion
- HTTP server for webhook on WEBHOOK_PORT
- Menu button setup on start

Config (env vars):
- REDIS_URL - for BullMQ
- SENTRY_DSN_BOT - for error tracking
- WEBHOOK_ENABLED, WEBHOOK_DOMAIN, WEBHOOK_PATH, WEBHOOK_PORT
