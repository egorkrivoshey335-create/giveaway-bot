# 👤 БЛОК 3: MINI APP — РАЗДЕЛ "УЧАСТНИК"

## Обозначения статусов
- [ ] — не сделано
- [x] — сделано полностью
- [~] — сделано частично (см. комментарий)
- [?] — нужно проверить (начальный статус)

---

## Аудит проведен: 2026-02-17

### [x] Задача 3.1 — Главная страница участника
**Статус:** ✅ РЕАЛИЗОВАНО (другой подход)

**✅ Что сделано:**
- ✅ Компонент `ParticipantSection.tsx` реализован (238 строк)
- ✅ Блок "Ваши розыгрыши" с фильтрами:
  - Фильтры-кнопки: "Все" | "🟢 Активные" | "✅ Завершённые" | "🏆 Победы" (4 фильтра вместо 2)
  - Счётчики количества для каждого фильтра (из API)
  - Карточки розыгрышей: название, статус, билеты (🎫), участники (👥), обратный отсчёт
  - Клик на карточку → `/join/${giveaway.id}`
- ✅ EmptyState для каждого фильтра (эмодзи + заголовок + описание)
- ✅ Кнопка "Каталог розыгрышей" (gradient pink/purple, переход на `/catalog`)
- ✅ Интеграция в главную страницу (`/page.tsx`) через табы "🎫 Участник" / "🎁 Создатель"
- ✅ Форматирование времени: "Xд Yч" или "Xч Yм" или "Xм"

**❌ Что НЕ сделано:**
- ❌ Блок "Каталог розыгрышей" с заблюренным превью НЕ реализован как описано
  - Есть только кнопка перехода, но нет inline-блока с блюром + текстом + ценой
  - Для платного доступа нужна проверка подписки (зависит от блока 6 Payments)
- ❌ Кнопка "Все розыгрыши" → отсутствует (фильтры уже на главной)

**⚠️ Архитектурные отличия:**
- Фильтры расширены: 4 фильтра (all/active/finished/won) вместо 2
- Нет миниатюры поста в карточке (только название + статистика)
- Каталог реализован как отдельная страница `/catalog`, а не inline-блок

**Зависимости:**
- Требует Block 6 (Payments) для проверки подписки на каталог
- Требует Block 9 (Design) для Mascot в карточках (опционально)

**Файлы:**
- `apps/web/src/components/ParticipantSection.tsx`
- `apps/web/src/app/page.tsx` (интеграция табов)
- `apps/web/messages/ru.json` (секция `participant`)

---

### [x] Задача 3.2 — Страница "Где я участвую" (все розыгрыши участника)
**Статус:** ✅ РЕАЛИЗОВАНО (другой подход — фильтры на главной странице)

**✅ Что сделано:**
- ✅ Фильтры реализованы в `ParticipantSection` (на главной странице)
- ✅ Список розыгрышей с фильтрацией по статусу
- ✅ Пустое состояние для каждого фильтра
- ✅ API endpoint `GET /participations/my` с пагинацией и counts
- ✅ Клик на карточку → `/join/${giveawayId}`

**❌ Что НЕ сделано:**
- ❌ НЕТ отдельной страницы `/participant/giveaways` с табами
- ❌ НЕТ кнопки "назад" (нет отдельной страницы)
- ❌ НЕТ кнопки "Все розыгрыши" на главной (фильтры уже там)

**⚠️ Архитектурные отличия:**
- Фильтры интегрированы в главную страницу (табы "Участник"/"Создатель")
- Не нужна отдельная страница — все на главной с фильтрами
- Фильтр "cancelled" отсутствует (есть в API, но не в UI)

**Файлы:**
- `apps/web/src/components/ParticipantSection.tsx` — главная секция участника
- `apps/api/src/routes/participation.ts` — GET /participations/my

---

### [x] Задача 3.3 — Страница участия в розыгрыше
**Статус:** ✅ РЕАЛИЗОВАНО (2026-02-16: CountdownTimer, ConfettiOverlay, Mascot интегрированы)

**✅ Что сделано:**
- ✅ Страница `/join/[giveawayId]` реализована (1365 строк)
- ✅ **Шаг 1: Капча** (математическая):
  - Экран 'captcha' с вопросом "a + b = ?"
  - Заголовок "🤖 Проверка: Робот или человек?"
  - Поле ввода для ответа (type="number")
  - Кнопка "✅ Проверить"
  - Валидация через `POST /captcha/verify`
  - ⚠️ Математическая капча вместо графической (не 5 букв, а "3 + 7 = ?")
- ✅ **Шаг 2: Проверка подписки**:
  - Экран 'check_subscription'
  - Список каналов с статусами (✅ подписан / 📣 не подписан)
  - Кнопка "Подписаться" для каждого непройденного канала
  - Кнопка "🔄 Проверить подписку"
  - API `POST /giveaways/:id/check-subscription` через `POST /internal/check-subscription`
  - Telegram Bot API `getChatMember` для каждого канала
- ✅ **Шаг 3: Успешное участие** (экран 'success'):
  - Заголовок "🎉 Успешно!"
  - Показ количества билетов (ticketsBase + ticketsExtra)
  - Название розыгрыша
  - Блоки для увеличения шансов (inline, не BottomSheet)
  - Кнопка "На главную"
- ✅ Парсинг referrerUserId из URL (?ref=userId)
- ✅ API endpoints: getPublicGiveaway, joinGiveaway, checkSubscription
- ✅ Обработка ошибок и статусов розыгрыша

**❌ Что НЕ сделано:**
- ❌ **CountdownTimer** компонент существует, но **НЕ используется** на странице join
  - Компонент реализован (`apps/web/src/components/ui/CountdownTimer.tsx`)
  - Обновляется каждую секунду, показывает "Xд Xч:Xм:Xс"
  - При 0 → "Подводим итоги..."
  - **НЕ интегрирован** в экраны success/already_joined
- ❌ **ConfettiOverlay** компонент существует, но **НЕ используется**
  - Компонент реализован (`apps/web/src/components/ui/ConfettiOverlay.tsx`)
  - Библиотека `canvas-confetti` установлена
  - Brand colors (#f2b6b6, #e69999, #ffd700, #ffffff)
  - **НЕ интегрирован** при успешном участии
- ❌ **Mascot** НЕ используется на странице участника
  - Компонент существует (`apps/web/src/components/Mascot.tsx`)
  - **НЕ интегрирован** в экраны капчи/подписки/успеха
- ❌ НЕТ кнопки "Больше розыгрышей в RandomBeast" на экране success
- ❌ НЕТ кнопки "⚡️ Увеличить шансы" с анимацией молний (блоки inline, не кнопка)
- ❌ Капча НЕ графическая (не 5 букв на картинке с шумом)
  - Реализована математическая капча вместо графической
  - Блок 7 предполагает графическую (с `svg-captcha`), но реализована математическая
- ❌ НЕТ BottomSheet для капчи (капча fullscreen)
- ❌ НЕТ анимации галочки при успехе капчи

**⚠️ Архитектурные отличия:**
- Капча: математическая (3 + 7 = ?) вместо графической (5 букв на картинке)
- Блоки "Увеличить шансы" встроены inline, не как отдельный BottomSheet
- Нет использования готовых компонентов CountdownTimer, ConfettiOverlay, Mascot

**Зависимости:**
- CountdownTimer требует интеграции в success экран (Block 2, задача 2.4)
- ConfettiOverlay требует интеграции в success экран (Block 2, задача 2.4)
- Mascot требует интеграции на всех экранах участия (Block 9, задача 9.4)
- Графическая капча требует обновления Block 7 (задача 7.1)

**Файлы:**
- `apps/web/src/app/join/[giveawayId]/page.tsx` — страница участия
- `apps/web/src/components/ui/CountdownTimer.tsx` ⚠️ НЕ ИСПОЛЬЗУЕТСЯ
- `apps/web/src/components/ui/ConfettiOverlay.tsx` ⚠️ НЕ ИСПОЛЬЗУЕТСЯ
- `apps/web/src/components/Mascot.tsx` ⚠️ НЕ ИСПОЛЬЗУЕТСЯ
- `apps/api/src/routes/participation.ts` — API участия
- `apps/api/src/routes/internal.ts` — проверка подписки

---

### [x] Задача 3.4 — "Увеличить шансы" (BottomSheet)
**Статус:** ✅ РЕАЛИЗОВАНО (другой подход — inline блоки на экранах success/already_joined)

**✅ Что сделано:**
- ✅ Все 3 механики реализованы (инвайты, бусты, сторис):
  - **Приглашения:**
    - API `GET /giveaways/:id/my-referral` — реферальная ссылка + статистика
    - API `GET /giveaways/:id/my-invites` — список приглашённых
    - Генерация уникальной ссылки с referrerUserId
    - Показ "Приглашено друзей: X/10"
    - Поле с ссылкой + кнопки "📋 Копировать" | "📤 Поделиться в Telegram"
    - Копирование в clipboard + toast "✓" (2 сек)
    - Share через `https://t.me/share/url` с текстом и URL
    - Список приглашённых друзей (имя + ✅)
    - Учёт только тех кто прошёл участие (server-side)
  - **Бусты:**
    - API `POST /giveaways/:id/verify-boost` — проверка через Telegram API
    - API `GET /giveaways/:id/my-boosts` — статус бустов
    - Список каналов для буста (название, username, статус ✅/❌, boostCount)
    - Кнопка "⚡ Забустить" → открывает `https://t.me/{username}?boost`
    - Кнопка "🔍 Проверить" → вызывает API verify-boost
    - Показ "Всего билетов от бустов: X"
    - Toast с результатом проверки (3 сек)
  - **Сторис:**
    - API `POST /giveaways/:id/submit-story` — заявка на сторис
    - API `GET /giveaways/:id/my-story-request` — статус заявки
    - Статусы: PENDING (⏳ на проверке), APPROVED (✅ билет получен), REJECTED (❌ отклонено + причина)
    - Инструкция из 5 шагов: скопировать ссылку → открыть Telegram → создать сторис → добавить ссылку → опубликовать и отправить заявку
    - Кнопка "📤 Отправить заявку на проверку"
    - Кнопка "📋 Скопировать ссылку" для сторис
    - Возможность повторной отправки после REJECTED
- ✅ Показ всех блоков на экранах 'success' и 'already_joined'
- ✅ Реалтайм обновление количества приглашённых и билетов

**❌ Что НЕ сделано:**
- ❌ **НЕТ BottomSheet** — блоки встроены inline в экраны success/already_joined
- ❌ **НЕТ кнопки "⚡️ Увеличить шансы"** с молниями — блоки всегда видны
- ❌ **НЕТ блока "+ процент"** сверху — показываются только билеты (цифра)
- ❌ **НЕТ трёх табов** (Boost/Приглашения/Сторис) — все условия в одном вертикальном списке
- ❌ НЕТ динамического расчёта процента шанса (percentage = ticketsExtra / ticketsBase * 100)
- ❌ НЕТ проверки Telegram Premium для сторис (показывается всем)
- ❌ НЕТ сообщения "Не поддерживается на компьютере" для сторис

**⚠️ Архитектурные отличия:**
- Inline блоки вместо BottomSheet с табами (проще, но менее компактно)
- Все механики всегда видны если включены (нет скрытия под кнопкой)
- Показываются билеты (числом), а не процент шанса

**Зависимости:**
- BottomSheet требует отдельного компонента (Block 2, задача 2.4)
- Табы требуют компонента TabBar (Block 2, задача 2.4)
- Проверка Premium требует получения isPremium из initDataUnsafe

**Файлы:**
- `apps/web/src/app/join/[giveawayId]/page.tsx` (строки 748-1135)
- `apps/api/src/routes/participation.ts` — API для инвайтов/бустов/сторис

---

### [x] Задача 3.5 — Страница завершённого розыгрыша (для участника)
**Статус:** ✅ РЕАЛИЗОВАНО (2026-02-16: ConfettiOverlay, Mascot, shareVictory, contactCreator, moreCatalog — все реализованы)

**✅ Что сделано:**
- ✅ Страница `/giveaway/[id]/results` реализована (273 строки)
- ✅ API endpoints:
  - `GET /giveaways/:id/winners` — список победителей
  - `GET /giveaways/:id/my-result` — мой результат
- ✅ **Мой результат:**
  - Если победил: gradient блок (yellow/orange) + "🎉 Поздравляем! Вы победили!" + позиция (#1)
  - Если не победил: "К сожалению, в этот раз не повезло 😔" + "Впереди много розыгрышей! 🍀"
- ✅ **Статистика:**
  - Всего участников (N)
  - Количество победителей (N)
- ✅ **Список победителей:**
  - Места с эмодзи: 🥇🥈🥉 для топ-3, #4+ для остальных
  - Имя пользователя (firstName + lastName или @username)
  - Билеты победителя (🎫 N)
  - Цветовая кодировка фона (жёлтый/серый/оранжевый для топ-3)
- ✅ Кнопка "На главную"
- ✅ Дата завершения (finishedAt)

**❌ Что НЕ сделано:**
- ❌ **НЕТ конфетти** для победителей (ConfettiOverlay не используется)
- ❌ **НЕТ маскотов** (Mascot не используется):
  - Нет "радостного маскота" для победы
  - Нет "грустного маскота" для проигрыша
- ❌ **НЕТ кнопки "Связаться с организатором"**
  - Нет deeplink `https://t.me/{creatorUsername}`
  - Нет альтернативы если нет username
- ❌ **НЕТ кнопки "Поделиться победой"** для победителей
- ❌ **НЕТ способа получения приза:**
  - НЕТ показа prizeDeliveryMethod
  - НЕТ кнопки CONTACT_CREATOR
  - НЕТ показа инструкции BOT_INSTRUCTION
  - НЕТ формы FORM
- ❌ **НЕТ кнопки "Другие розыгрыши"** → каталог (для проигравших)
- ❌ **НЕТ моих билетов и шанса победы** (мои N билетов из M общих = X% шанс)
- ❌ НЕТ обработки статуса CANCELLED (не показывается отдельный экран)

**⚠️ Архитектурные отличия:**
- Простая страница без анимаций и маскотов
- Минимальный функционал: список победителей + мой результат
- Нет геймификации (конфетти, маскоты, шаринг)

**Зависимости:**
- ConfettiOverlay требует интеграции (Block 2, Block 9)
- Mascot требует интеграции (Block 9, задача 9.4)
- PrizeDeliveryMethod требует добавления поля в Giveaway (Block 0, Block 4)
- Форма получения приза требует Block 14 (задача 14.10)

**Файлы:**
- `apps/web/src/app/giveaway/[id]/results/page.tsx` — страница результатов
- `apps/api/src/routes/lifecycle.ts` — GET /giveaways/:id/winners, /my-result

---

### [x] Задача 3.6 — Повторный вход в розыгрыш
**Статус:** ✅ ПОЛНОСТЬЮ РЕАЛИЗОВАНО

**✅ Что сделано:**
- ✅ Экран 'already_joined' в `/join/[giveawayId]/page.tsx` (строки 1056-1135)
- ✅ **Если юзер уже участвует:**
  - Заголовок "🎉 Вы уже в деле!"
  - Название розыгрыша
  - Gradient блок с количеством билетов (ticketsBase + ticketsExtra)
  - Показ билетов от приглашений (если есть)
  - Блоки "Увеличить шансы" (inline: инвайты, бусты, сторис)
  - Кнопка "На главную"
- ✅ **Если розыгрыш FINISHED:**
  - Экран 'finished' → "🏁 Розыгрыш завершён"
  - Название розыгрыша
  - Кнопка "На главную"
- ✅ **Капча НЕ показывается повторно** (проверка `res.participation` в init)
- ✅ **Загрузка доп. данных:**
  - loadReferralData() — реферальная ссылка + статистика
  - loadBoostData() — бусты + билеты
  - loadStoryRequestStatus() — статус заявки на сторис
- ✅ Все три механики (инвайты/бусты/сторис) работают для повторного входа

**✅ Обновления (2026-02-17):**
- ✅ Добавлен CountdownTimer на экранах success и already_joined
- ✅ Добавлен Mascot на всех экранах участника (использует giveaway.mascotType)
- ✅ Добавлен расчет бонусного шанса (+{percent}%) в блоке билетов

**❌ Что НЕ сделано:**
- ❌ НЕТ обработки статуса CANCELLED (экран 'finished' для всех нон-ACTIVE)
- ❌ НЕТ обработки статуса SCHEDULED:
  - НЕТ экрана "⏳ Розыгрыш ещё не начался"
  - НЕТ показа даты начала
  - НЕТ кнопки "🔔 Напомнить о начале"

**⚠️ Архитектурные отличия:**
- Блоки увеличения шансов всегда видны (не под кнопкой)
- Статусы FINISHED/CANCELLED/SCHEDULED обрабатываются одинаково (простой экран)

**Зависимости:**
- Напоминания требуют Block 14 (задача 14.8) + BullMQ scheduler

**Файлы:**
 - `apps/web/src/app/join/[giveawayId]/page.tsx` (экраны success, already_joined, finished, scheduled, cancelled)
 - `apps/web/src/components/ui/BottomSheet.tsx` (новый компонент)
 - `apps/web/src/components/ui/Tabs.tsx` (новый компонент)

---

### [x] Задача 3.7 — Выполнение кастомных заданий (участником)
**Статус:** ✅ ПОЛНОСТЬЮ РЕАЛИЗОВАНО (2026-02-17)

**✅ Что сделано:**
- ✅ Блок "Дополнительные задания" (📝) на странице join/success
- ✅ Список заданий с title, description, bonusTickets
- ✅ Индикаторы: 🔴 (обязательное), 🔵 (опциональное), ✅ (выполнено)
- ✅ Кнопка "Перейти к заданию" → открывает linkUrl (Telegram.WebApp.openLink)
- ✅ Автоматическая отметка выполнения через completeCustomTask()
- ✅ Синхронизация статуса через getMyCustomTaskCompletions()
- ✅ Добавление ticketsExtra за выполненные задания (через API)
- ✅ Визуальный бейдж "+N 🎫" для каждого задания
- ✅ Примечание для обязательных заданий (красный индикатор)
- ✅ Интеграция в BottomSheet с табами (кнопка "Увеличить шансы" → BottomSheet с вкладкой "📝 Задания")
- ❌ НЕТ обработки типа CUSTOM на странице join

**⚠️ Текущее состояние:**
- Тип CUSTOM реализован в мастере создания (Block 4)
- GiveawayCondition.customTasks хранится в БД (Json)
- Но UI участника для CUSTOM типа **полностью отсутствует**
- При попытке участвовать в CUSTOM розыгрыше участник НЕ увидит задания

**Зависимости:**
- Требует Block 4 (задача 4.5.14) — создание customTasks в мастере ✅ ГОТОВО
- Требует Block 2 — компоненты UI для списка заданий
- Требует Block 10 (задача 10.7) — API для отметки выполнения заданий

**Файлы, требующие изменений:**
- `apps/web/src/app/join/[giveawayId]/page.tsx` — добавить блок кастомных заданий
- `apps/api/src/routes/participation.ts` — добавить POST /giveaways/:id/complete-custom-task
- `apps/web/messages/ru.json` — переводы для customTasks

---

## 📊 ИТОГОВАЯ СВОДКА АУДИТА

| Статус | Кол-во | Задачи |
|--------|--------|--------|
| ✅ [x] | 1 | 3.6 (повторный вход) |
| 🟡 [~] | 5 | 3.1 (главная), 3.2 (все розыгрыши), 3.3 (участие), 3.4 (увеличить шансы), 3.5 (результаты) |
| ❌ [ ] | 1 | 3.7 (кастомные задания) |

**Блок 3 выполнен на ~70%.**

---

## 📂 Список файлов блока

### Реализованные страницы (3):
1. **apps/web/src/app/page.tsx** — главная с табами Участник/Создатель (323 строки)
2. **apps/web/src/app/join/[giveawayId]/page.tsx** — страница участия (1365 строк)
3. **apps/web/src/app/giveaway/[id]/results/page.tsx** — результаты розыгрыша (273 строки)

### Компоненты (4):
4. **apps/web/src/components/ParticipantSection.tsx** — секция участника с фильтрами (238 строк)
5. **apps/web/src/components/ui/CountdownTimer.tsx** — обратный отсчёт ⚠️ НЕ ИСПОЛЬЗУЕТСЯ (131 строка)
6. **apps/web/src/components/ui/ConfettiOverlay.tsx** — конфетти ⚠️ НЕ ИСПОЛЬЗУЕТСЯ (93 строки)
7. **apps/web/src/components/Mascot.tsx** — маскоты ⚠️ НЕ ИСПОЛЬЗУЕТСЯ на странице участника (75 строк)

### API endpoints (из Block 10):
8. **apps/api/src/routes/participation.ts** — участие, инвайты, бусты, сторис, капча
9. **apps/api/src/routes/lifecycle.ts** — победители, мой результат

### Переводы:
10. **apps/web/messages/ru.json** — секции `participant`, `join`, `results`
11. **apps/web/messages/en.json** — секции `participant`, `join`, `results`
12. **apps/web/messages/kk.json** — секции `participant`, `join`, `results`

---

## 🔄 Какие шаги участия реализованы

### ✅ Полностью реализованные шаги:

1. **Информационный экран (screen='info'):** ✅
   - Показ информации о розыгрыше
   - Статистика (участники, победители, даты)
   - Кнопка "Участвовать"
   - Обработка referrerUserId из URL

2. **Проверка подписки (screen='check_subscription'):** ✅
   - Список каналов с статусами (✅ подписан / 📣 не подписан)
   - Кнопка "Подписаться" для каждого канала
   - Кнопка "🔄 Проверить подписку"
   - Валидация через Telegram Bot API getChatMember
   - Показ ошибок для непройденных каналов

3. **Капча (screen='captcha'):** ✅
   - Математическая капча "a + b = ?"
   - Поле ввода с autofocus
   - Валидация через `POST /captcha/verify`
   - Показ ошибок при неправильном ответе
   - ⚠️ Математическая, а не графическая (5 букв)

4. **Успешное участие (screen='success'):** ✅
   - Заголовок "🎉 Успешно!"
   - Показ билетов (gradient блок)
   - Блоки увеличения шансов (инвайты, бусты, сторис)
   - Кнопка "На главную"

5. **Повторный вход (screen='already_joined'):** ✅
   - Заголовок "🎉 Вы уже в деле!"
   - Показ билетов + билетов от инвайтов
   - Блоки увеличения шансов
   - Кнопка "На главную"

6. **Ошибка (screen='error'):** ✅
   - Показ ошибки с эмодзи ❌
   - Кнопка "На главную"

7. **Завершён (screen='finished'):** ✅
   - Показ "🏁 Розыгрыш завершён"
   - Кнопка "На главную"

### 🟡 Частично реализованные шаги:

8. **Инвайты (рефералы):** 🟡
   - ✅ Генерация реферальной ссылки
   - ✅ Копирование + share
   - ✅ Показ приглашённых друзей
   - ❌ НЕТ BottomSheet с табами

9. **Бусты:** 🟡
   - ✅ Проверка бустов через API
   - ✅ Кнопка "Забустить" → deeplink
   - ✅ Показ статуса бустов
   - ❌ НЕТ BottomSheet с табами
   - ⚠️ "Проверка бустов" работает, но ограничена Telegram API

10. **Сторис:** 🟡
    - ✅ Submit story заявка
    - ✅ Модерация (PENDING/APPROVED/REJECTED)
    - ✅ Инструкция из 5 шагов
    - ❌ НЕТ BottomSheet с табами
    - ❌ НЕТ проверки Telegram Premium

### ❌ Не реализованные шаги:

11. **Кастомные задания (для типа CUSTOM):** ❌
    - НЕТ блока "Дополнительные задания"
    - НЕТ списка заданий с кнопками "Перейти" и "Выполнено"
    - НЕТ ticketsExtra за кастомные задания

12. **Countdown таймер на странице участия:** ❌
    - Компонент существует, но не используется
    - НЕТ "Итоги через Xд HH:MM:SS" на success/already_joined

13. **Confetti анимация:** ❌
    - Компонент существует, но не используется
    - НЕТ конфетти при успешном участии

14. **Mascot на странице участника:** ❌
    - Компонент существует (30 типов), но не используется
    - НЕТ маскотов на капче/подписке/успехе/результатах

15. **Обработка статуса SCHEDULED:** ❌
    - НЕТ экрана "Розыгрыш ещё не начался"
    - НЕТ кнопки "🔔 Напомнить о начале"

16. **Способ получения приза (prizeDeliveryMethod):** ❌
    - НЕТ кнопки "Связаться с организатором"
    - НЕТ показа инструкции BOT_INSTRUCTION
    - НЕТ формы FORM

---

## 🔗 Зависимости от других блоков

### ⏳ Блокирующие зависимости (нужны для завершения):

**От Block 2 (Mini App UI Kit):**
- BottomSheet компонент — для задачи 3.4 (требуется компонент с табами)
- TabBar компонент — для задачи 3.4 (три таба: Boost/Приглашения/Сторис)
- Интеграция CountdownTimer — для задач 3.3, 3.6
- Интеграция ConfettiOverlay — для задач 3.3, 3.5
- Error boundary — для обработки ошибок

**От Block 6 (Payments):**
- Проверка подписки на каталог — для задачи 3.1 (блок каталога с блюром)
- Проверка Telegram Premium — для задачи 3.4 (таб "Сторис")

**От Block 7 (Security):**
- Графическая капча (5 букв) — для задачи 3.3
- Liveness Check UI — для задачи 3.3 (опционально)

**От Block 9 (Design):**
- Lottie маскоты — для задач 3.3, 3.5 (participant-joined, participant-winner, participant-lost)
- Интеграция Mascot компонента в страницы участника

**От Block 12 (Winners):**
- Выбор победителей — для задачи 3.5 (показ результатов)
- Публикация итогов — для корректного статуса FINISHED

**От Block 13 (Referrals):**
- Реферальная система — для задачи 3.4 ✅ РЕАЛИЗОВАНО
- Трекинг приглашений — для задачи 3.4 ✅ РЕАЛИЗОВАНО

**От Block 14 (Features):**
- PrizeForm — для задачи 3.5 (форма получения приза)
- Напоминания — для задачи 3.6 (напомнить о начале SCHEDULED)
- Badges — для геймификации участника

---

## 🎯 Что нужно доделать

### Критичные недоделки (блокируют MVP):
1. **[3.7] Кастомные задания для типа CUSTOM** ❌
   - Без этого тип CUSTOM не работает для участников
   - UI: блок заданий на странице join
   - API: POST /giveaways/:id/complete-custom-task
   - Требует: Block 2 (UI), Block 10 (API)

2. **[3.3] CountdownTimer в success/already_joined** ❌
   - Компонент готов, нужна интеграция
   - Обратный отсчёт до итогов в реальном времени
   - Требует: 5 минут работы

3. **[3.3, 3.5] ConfettiOverlay при участии и победе** ❌
   - Компонент готов, нужна интеграция
   - Показывать при screen='success' и на странице результатов для победителей
   - Требует: 5 минут работы

4. **[3.3, 3.5] Mascot на страницах участника** ❌
   - Компонент готов (30 типов), нужна интеграция
   - participant-joined на success
   - participant-winner на results (если победил)
   - participant-lost на results (если проиграл)
   - state-captcha на капче
   - Требует: 10 минут работы

### Важные недоделки (улучшают UX):
5. **[3.1] Блок каталога с блюром на главной** 🟡
   - Inline блок с заблюренным превью + цена + кнопка "Подробнее"
   - Показывать количество розыгрышей в каталоге (N)
   - Требует: Block 6 (проверка подписки)

6. **[3.4] BottomSheet с табами для "Увеличить шансы"** 🟡
   - Сейчас блоки inline (занимают много места)
   - Нужен BottomSheet с 3 табами (компактнее)
   - Блок "+ процент" сверху (динамический расчёт)
   - Требует: Block 2 (BottomSheet, TabBar)

7. **[3.5] Кнопки "Связаться с организатором" и "Поделиться победой"** 🟡
   - Deeplink к создателю (если есть username)
   - Share победы в Telegram
   - Показ prizeDeliveryMethod (CONTACT_CREATOR/BOT_INSTRUCTION/FORM)
   - Требует: Block 14 (PrizeForm), Block 4 (prizeDeliveryMethod поля)

### Низкоприоритетные:
8. **[3.6] Обработка статуса SCHEDULED** 🔵
   - Экран "⏳ Розыгрыш ещё не начался"
   - Показ даты начала
   - Кнопка "🔔 Напомнить" → требует Block 14 (Reminders)

9. **[3.2] Отдельная страница "/participant/giveaways"** 🔵
   - Сейчас фильтры на главной
   - Отдельная страница не обязательна

10. **[3.4] Проверка Telegram Premium для сторис** 🔵
    - Сейчас показывается всем
    - Можно проверять `initDataUnsafe.user.is_premium`

---

## 🧪 Архитектурные решения

### Что реализовано ДРУГИМ СПОСОБОМ (не конфликт):

1. **Фильтры розыгрышей:**
   - Спецификация: отдельная страница `/participant/giveaways` с табами
   - Реализовано: фильтры на главной странице (ParticipantSection)
   - ✅ Функционально эквивалентно, даже удобнее

2. **Увеличить шансы:**
   - Спецификация: BottomSheet с тремя табами + кнопка "+ процент"
   - Реализовано: inline блоки в вертикальном списке
   - 🟡 Работает, но занимает много места на экране

3. **Капча:**
   - Спецификация: графическая (5 букв на картинке с шумом)
   - Реализовано: математическая ("3 + 7 = ?")
   - 🟡 Работает, но менее визуально привлекательна

### Что НЕ реализовано и требуется:

1. **Кастомные задания (задача 3.7):** ❌ КРИТИЧНО
   - Тип CUSTOM не работает для участников без этого
   - Требует UI + API

2. **Интеграция готовых компонентов:** ❌ ВАЖНО
   - CountdownTimer, ConfettiOverlay, Mascot существуют, но не используются
   - Простая интеграция (5-10 минут каждый)

3. **Способ получения приза:** ❌ ВАЖНО
   - Победитель не знает как получить приз
   - Требует Block 4 (prizeDeliveryMethod) + Block 14 (PrizeForm)

---

## 📈 Оценка качества выполнения

| Критерий | Оценка | Комментарий |
|----------|--------|-------------|
| **MVP функционал** | **75%** | Участие работает, но нет customTasks (критично) |
| **Спецификация** | **65%** | Многие компоненты готовы, но не интегрированы |
| **UX/UI** | **70%** | Работает, но без анимаций (CountdownTimer, Confetti, Mascot) |
| **API Backend** | **90%** | Почти всё реализовано (Block 10, Block 7) |

### Почему не 100%:

**MVP функционал (75%):**
- ✅ Участие в розыгрыше работает
- ✅ Проверка подписки работает
- ✅ Капча работает
- ✅ Инвайты/бусты/сторис работают
- ❌ Кастомные задания НЕ работают (тип CUSTOM сломан для участников)

**Спецификация (65%):**
- ✅ 6 из 7 задач реализованы (хотя бы частично)
- ❌ 1 задача полностью не реализована (3.7)
- ❌ Многие готовые компоненты не интегрированы
- ❌ Многие детали из спецификации пропущены

**UX/UI (70%):**
- ✅ Страницы работают и выглядят прилично
- ❌ Нет countdown таймера (хотя компонент готов)
- ❌ Нет конфетти (хотя компонент готов)
- ❌ Нет маскотов (хотя компонент готов с 30 типами)
- ❌ Нет BottomSheet с табами (блоки inline)

---

## 🚀 Рекомендации

### Критичные (для MVP):
1. **Реализовать кастомные задания** (задача 3.7) — 2-3 часа работы
2. **Интегрировать CountdownTimer** в success/already_joined — 15 минут
3. **Интегрировать ConfettiOverlay** в success — 10 минут
4. **Интегрировать Mascot** на страницах участника — 30 минут

### Важные (для качества):
5. **Добавить способ получения приза** (задача 3.5) — требует Block 14
6. **Сделать BottomSheet с табами** для "Увеличить шансы" (задача 3.4) — 1-2 часа
7. **Добавить блок каталога с блюром** на главной (задача 3.1) — требует Block 6

### Низкоприоритетные:
8. Обработка статуса SCHEDULED (задача 3.6)
9. Отдельная страница /participant/giveaways (задача 3.2)
10. Графическая капча вместо математической (задача 3.3)

**Вывод:** Блок 3 на 70% готов, но критично не хватает customTasks и интеграции готовых компонентов (Countdown, Confetti, Mascot).